---
title: "[알고리즘 프로그래머스] 행렬의 덧셈  (LEVEL 1)"
tag:
- 알고리즘
- 프로그래머스 LEVEL1
categories:
- algorithm
---

# (문제) 행렬의 덧셈  
---

행렬의 덧셈은 행과 열의 크기가 같은 두 행렬의 같은 행, 같은 열의 값을 서로 더한 결과가 됩니다.

2개의 행렬 arr1과 arr2를 입력받아, 행렬 덧셈의 결과를 반환하는 함수, solution을 완성해주세요.

> **입력설명**

행렬 arr1, arr2의 행과 열의 길이는 500을 넘지 않습니다.

> **출력설명**

2개의 행렬 arr1과 arr2를 입력받아, 행렬 덧셈의 결과를 반환

> **테스트케이스**
 

| 입력예제 | 출력예제 |
| -------- | -------- | 
| [[1,2],[2,3]] [[3,4],[5,6]] | [[4,6],[7,9]] | 
| [[1],[2]] [[3],[4]] | [[4],[6]] | 

---
# 해결방법

행렬의 바깥쪽 개수와 안쪽 개 수 만큼 반복문을 돌려 둘의 요소들을 더해 기존의 값에 저장한다.

---
# 코드
```python
arr1 = [[1, 2], [2, 3]]
arr2 = [[3, 4], [5, 6]]

out_length = len(arr1)
in_length = len(arr1[0])
    
for i in range(out_length):
    for j in range(in_length):
        arr1[i][j] = arr1[i][j] + arr2[i][j]

print(arr1)
```
